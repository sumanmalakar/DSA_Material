#include <stdio.h>
int main()
{
  int age;
  printf("Enter the your age\n");
  scanf("%d", &age);

  (age >= 90) ? printf("you are above 90, you cannot drive \n") : printf("You can dirve \n");

  if(age = 50){
    printf("Half Century \n");
  }

  return 0;
}