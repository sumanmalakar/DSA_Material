#include<bits/stdc++.h>
using namespace std; 

 int main(){

   string str = "the sky is blue";
      
    //  int f = 0, n = str.length();

    //  for(int i=0; i<str.length()/2; i++){
    //    swap(str[i], str[n-i-1]);
    //  }
   string s = " sia";
    str += s;

   cout<<str;

       return 0;
}